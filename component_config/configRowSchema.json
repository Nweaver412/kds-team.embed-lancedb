{
  "type": "object",
  "title": "Embedding Configuration",
  "required": [
    "embedColumn"
  ],
  "properties": {
    "embedColumn": {
      "type": "string",
      "title": "Column to Embed",
      "description": "Select the column to be embedded",
      "propertyOrder": 1,
      "enum": [],
      "format": "select",
      "options": {
        "async": {
          "label": "Load Columns",
          "action": "listColumns",
          "autoload": []
        }
      }
    }
  }
}